<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.rozkocha.szymon.magazyn">

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name=".activities.CategoriesActivity"
            android:label="@string/categories" />
        <activity
            android:name=".activities.CategoryActivity"
            android:label="@string/category" />
        <activity
            android:name=".activities.ProvidersActivity"
            android:label="@string/providers"></activity>
        <activity
            android:name=".activities.ProviderActivity"
            android:label="@string/provider" />
        <activity
            android:name=".activities.ArticlesActivity"
            android:label="@string/articles"></activity>
        <activity
            android:name=".activities.ArticleActivity"
            android:label="@string/article" />
        <activity
            android:name=".activities.SettingsActivities.SettingsActivity"
            android:label="Settings" />
        <activity
            android:name=".activities.SettingsActivities.ColorsActivity"
            android:label="Colors" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>